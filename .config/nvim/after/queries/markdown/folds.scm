; Fold YAML frontmatter (identified likely as minus_metadata)
;; extends

(minus_metadata) @fold

; You can also add back other things you want folded, if desired:
; (fenced_code_block) @fold
; (indented_code_block) @fold

; Or leave it with just the frontmatter rule if that's all you want folded.
